apiVersion: v1
kind: Service
metadata:
  name: bookstore-bff-desktop
  namespace: bookstore-ns
spec:
  type: LoadBalancer
  selector:
    app: bookstore-bff-desktop
  ports:
    - port: 80
      targetPort: 80
