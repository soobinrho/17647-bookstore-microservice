apiVersion: v1
kind: Service
metadata:
  name: bookstore-crm-service-customers
  labels:
    run: bookstore-crm-service-customers
spec:
  ports:
  - port: 587
    protocol: TCP
  selector:
    run: bookstore-crm-service-customers
